{
  "name": "sop-tool",
  "version": "2.0.0",
  "description": "SOP Management Tool with serverless backend",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'No build step required'",
    "deploy": "vercel deploy --prod",
    "lint": "echo 'Add linter if needed'"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {
    "vercel": "^33.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
